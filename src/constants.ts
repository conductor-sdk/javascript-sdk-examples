const SUFFIX = `${process.env.NSUFFIX}`;
export const GET_USER_INFO =  `get_user_info${SUFFIX}`;
export const SEND_EMAIL = `send_email${SUFFIX}`;
export const SEND_SMS =  `send_sms${SUFFIX}`;

export const EMAIL_SEND_WORKFLOW = `email_send_workflow${SUFFIX}`;

export const EMAIL_SEND_WORKFLOW_COMPLEX = `email_send_workflow_complex${SUFFIX}`;
